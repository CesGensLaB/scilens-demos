# Run Compare csv files with ignore columns

## Instructions

1. `cd` to this folder
2. Run `cglblens run .`

## Explainations

 Cglblens is configured to ignore the column wth label "Label 3"  in our .csv files.
 In our report, we will have an error.